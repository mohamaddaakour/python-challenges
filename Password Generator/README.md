🔐 Password Generator (Python)

This Python program generates a customizable random password using letters, numbers, and symbols.
The user decides how many of each character type the password should contain, making it flexible and secure.

✨ Features

Choose how many:

Letters (uppercase + lowercase)

Numbers

Symbols

Randomized order for better security

Outputs a single strong password

Uses only built-in Python modules (no external dependencies)

🧠 How It Works

The user is prompted to enter:

Number of letters

Number of digits

Number of symbols

The program picks random characters from predefined lists.

It shuffles them to avoid predictable patterns.

The password is printed as a single string.